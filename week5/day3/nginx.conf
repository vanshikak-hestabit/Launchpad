events{}

http {
    upstream backend_cluster {
        server server-1:3000;
        server server-2:3000;
    }
    server {
        listen 80;

        location / {
            proxy_pass http://backend_cluster;
        }
    }
}